

server:
  port: 8881


spring:
  application:
    name: user-service
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:9996/test?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8
    username: root
    password: 1234
    hikari:
      max-lifetime: 160000
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    generate-ddl: true

#  eureka:
#    instance:
#      hostname: localhost
#      prefer-ip-address: true
#    client:
#      register-with-eureka: true
#      fetch-registry: true
#      service-url:
#        defaultZone: http://127.0.0.1:8761/eureka

  data:
    redis:
      host: localhost
      port: 6379


logging:
  level:
    com.c209.user: DEBUG




cool:
  sms:
    secretkey: ${COOL_SMS_SECRET_KEY}
    apikey: ${COOL_SMS_API_KEY}
    outgoing:
      number: ${PHONE_NUMBER}

